# ContextBridge — dependencies
# Version ranges rather than hard pins: the spec's original pins (anthropic==0.28.0)
# predate the Claude models this project targets and cannot call them.

# Core
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-dotenv>=1.0.1
pydantic>=2.7.1
python-multipart>=0.0.9

# LLM & AI
anthropic>=0.40.0
tiktoken>=0.7.0

# Vector DB (also supplies the ONNX MiniLM embedding fallback)
chromadb>=0.5.3

# Chunking
langchain-text-splitters>=0.2.2

# Document Parsing
PyMuPDF>=1.24.5
python-docx>=1.1.2
pandas>=2.2.2
openpyxl>=3.1.4

# Frontend
streamlit>=1.35.0
plotly>=5.22.0

# Utilities
httpx>=0.27.0
loguru>=0.7.2
numpy>=1.26.4

# Optional: highest-quality embeddings. Pulls in torch (~2GB).
# The embedder falls back to ChromaDB's ONNX MiniLM (same model, same 384 dims,
# no torch) and then to a deterministic hashing encoder if this is absent.
# sentence-transformers>=3.0.1

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
